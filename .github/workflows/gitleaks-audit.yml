name: Secret Scanning — Weekly Audit
on:
  schedule:
    - cron: '0 8 * * 1' # lunes 08:00 UTC (03:00 Colombia)
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  gitleaks-audit:
    uses: r2capital/github-actions-templates/.github/workflows/gitleaks-audit-reusable.yml@v1
    secrets: inherit
